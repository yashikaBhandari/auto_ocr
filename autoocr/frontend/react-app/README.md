# React Frontend Placeholder

Planned features:
- Upload PDF/images.
- Display per-page thumbnails (before/after toggle).
- Allow manual override toggles (deskew, enhance, binarize, etc.).
- Show module decision log (detection scores & applied flags).

Implementation will follow after backend endpoints stabilize.
